# Дані для завдання

# Список цілих чисел
numbers = [12, 7, 45, 3, 28, 15, 9, 33, 21]

# Другий список цілих чисел
numbers2 = [50, 60, 70]

# Список з повторюваними елементами
numbers_with_duplicates = [4, 2, 7, 2, 9, 4, 1, 7, 3]

# Словник з іменами та віком
people = {"Олена": 25, "Іван": 30, "Марія": 22, "Петро": 35, "Анна": 28, "Сергій": 40}

# Значення для видалення зі списку
value_to_remove = 45

# Значення для пошуку в словнику
value_to_find = 30

# Реалізуйте завдання тут

# Перевіряємо, чи список не є порожнім
if numbers:
    # 2. Обчислення результату
    # Беремо перший елемент списку (за індексом 0) як початкове максимальне значення
    max_number = numbers[0]
    
    # Проходимо по всіх елементах списку за допомогою циклу for
    for number in numbers:
        # Якщо поточне число більше за збережене максимальне, оновлюємо максимум
        if number > max_number:
            max_number = number
            
    # 3. Виведення результату
    print("Максимальний елемент списку:", max_number)
else:
    print("Ви ввели порожній список.")
