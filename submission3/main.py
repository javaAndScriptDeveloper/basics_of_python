# Дані для завдання

# Список цілих чисел
numbers = [12, 7, 45, 3, 28, 15, 9, 33, 21]

# Другий список цілих чисел
numbers2 = [50, 60, 70]

# Список з повторюваними елементами
numbers_with_duplicates = [4, 2, 7, 2, 9, 4, 1, 7, 3]

# Словник з іменами та віком
people = {"Олена": 25, "Іван": 30, "Марія": 22, "Петро": 35, "Анна": 28, "Сергій": 40}

# Значення для видалення зі списку
value_to_remove = 45

# Значення для пошуку в словнику
value_to_find = 30

# Реалізуйте завдання тут
# Припускаємо, що перший елемент є максимальним
max_value = numbers[0]

# Проходимо по всіх елементах списку
for num in numbers:
    if num > max_value:  # Якщо знайдено більше число
        max_value = num  # Оновлюємо максимальне значення

# Виводимо результат
print("Максимальний елемент списку numbers:", max_value)